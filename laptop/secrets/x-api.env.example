# Copy this file to x-api.env (same folder) and fill in real values.
# NEVER commit x-api.env to git.
#
# Create app at https://developer.x.com/ (sign in as @TenMinMine)
# App permissions: Read and Write, then REGENERATE Access Token + Secret.
# Writing posts requires paid / pay-per-use credits on X developer billing.

X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
